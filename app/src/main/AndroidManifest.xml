<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raf.lat3_akb11_10116469">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".ProfileFragment" />
        <activity android:name=".MediaFragment" />
        <activity android:name=".HomeFragment" />
        <activity android:name=".GalleryFragment" />
        <activity android:name=".DailyFragment" />
        <activity android:name=".PagerAdapterActivity" />
        <activity android:name=".SliderWalkthroughActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
